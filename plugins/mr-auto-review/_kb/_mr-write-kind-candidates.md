# Candidate `mr_write.kind` Enum Values

> Tipos de output que los agentes podrían querer producir durante una review que NO encajan en el enum actual (`issue|report|context|script`).
> Evaluar en Wave 2 si se integran al MCP server.

| Candidate kind | Use case | Evidence | Decision |
|---|---|---|---|

*(empty — populated by analysis)*
